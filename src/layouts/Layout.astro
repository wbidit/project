---
import GlowCursor from '../components/Cursor';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Bidit Raj - UI/UX Designer & Frontend Developer</title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Cursor Styles -->
    <link rel="stylesheet" href="/styles/glow-cursor.css" />
  </head>
  <body class="bg-dark-900">
    <GlowCursor client:load />
    <slot />

    <!-- Social Links/Other components would go here -->
  </body>
</html>

<style is:global>
  :root {
    --accent: 124, 58, 237;
    --accent-gradient: linear-gradient(45deg, rgb(var(--accent)), #2DD4BF 30%, #18181b 60%);
  }
  
  html {
    font-family: 'Space Grotesk', system-ui, sans-serif;
    background-color: #18181b;
    color: #f7f7f8;
    cursor: none !important;
  }
  
  body {
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    -webkit-tap-highlight-color: transparent;
  }

  /* Hide cursor for all elements */
  * {
    cursor: none !important;
  }

  ::selection {
    background: #7C3AED;
    color: #f7f7f8;
  }

  /* Mobile touch optimization */
  @media (hover: none) and (pointer: coarse) {
    html {
      cursor: default !important;
    }
    
    .custom-cursor, .cursor-dot {
      display: none !important;
    }
  }
</style>